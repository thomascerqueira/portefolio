{
  "name": "portefolio",
  "version": "1.0.0",
  "source": "index.html",
  "repository": "git@github.com:thomascerqueira/portefolio.git",
  "author": "Thomas Cerqueira <thomas.cerqueira@epitech.eu>",
  "license": "MIT",
  "scripts": {
    "start": "rm -rf dist && mkdir dist && cp -r public dist/public && parcel",
    "build": "rm -rf dist && mkdir dist && cp -r public dist/public && parcel build"
  },
  "dependencies": {
    "parcel": "^2.7.0"
  },
  "staticFiles": {
    "staticPath": ["public"]
 }
}
